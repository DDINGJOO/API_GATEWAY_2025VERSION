server:
  port: ${SERVER_PORT}
service:
  auth:
    url: ${AUTH_URL}
    port: ${AUTH_PORT}

  profile:
    url : ${PROFILE_URL}
    port: ${PROFILE_PORT}

  image:
    url: ${IMAGE_URL}
    port: ${IMAGE_PORT}

  article:
    url: ${ARTICLE_URL}
    port: ${ARTICLE_PORT}

  comment:
    url: ${COMMENT_URL}
    port: ${COMMENT_PORT}

  gaechu:
    url: ${GAECHU_URL}
    port: ${GAECHU_PORT}
  activity:
    url: ${ACTIVITY_URL}
    port: ${ACTIVITY_PORT}
  support:
    url: ${SUPPORT_URL}
    port: ${SUPPORT_PORT}

