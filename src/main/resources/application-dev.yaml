server:
  port: 8083
service:
  auth:
    url: ${AUTH_URL}
    port: ${AUTH_PORT}

  profile:
    url : ${PROFILE_URL}
    port: ${PROFILE_PORT}

  image:
    url: ${IMAGE_URL}
    port: ${IMAGE_PORT}
