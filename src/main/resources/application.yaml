spring:
  profiles:
    active: dev
  data:
    redis:
      repositories:
        enabled: false
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}

profile:
  cache:
    ttl-seconds: ${PROFILE_CACHE_TTL_SECONDS:3600}
